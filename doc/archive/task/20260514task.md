# 投資情報リポジトリ 改修タスクリスト（現フェーズ）

## 進捗サマリー（2026-05-17時点）

### 完了（直近の運用改善）

- [x] シナリオ投稿の重複抑止（同一銘柄・同一方向・同一tierの短時間再投稿）
- [x] 返信コマンド堅牢化（`entry lots=... price=...` / 不正フォーマットACK）
- [x] exit理由タグ保存（`tp/sl/time/manual`）
- [x] `paper-exit-timing` の mode別比較（`backtest/watch/live`）
- [x] 週次レビュー自動出力（`weekly-trade-watch-review`）
- [x] watch→trade 昇格ルール明文化（docs反映）
- [x] 品質ゲート自動調整（サンプル不足時の段階緩和）
- [x] Ladder運用導入（`strict/balanced/early/none`）
- [x] Ladder実績集計（週次レビューに `Ladder Effectiveness` 追加）
- [x] `none` の2分割（`none-high-score / none-low-score`）
- [x] Missing補完強化の一部（`check_daily_missing.py` に DB欠損/Discord未投稿検知を統合）

### 継続（次に進める）

- [x] Scheduler health専用script（`check_scheduler_health.py`）の実装/運用
- [x] Missing補完のDB欠損・未投稿検知の統合（閾値調整/誤検知低減のチューニング）
- [x] `execution_plan` テーブルと生成フロー（Phase 2/4）※初版実装
- [ ] Rank性能分析・Rule Calibration（Phase 3）※日次 `paper-trade-stats` に rank別T+5集計を追加済み
- [ ] Market Regime / Gap戦略検証（Phase 5）
- [x] 運用ドキュメント最終統合（catalog/doc整合）

### 保留（後回し）

- [ ] 板API連携
- [ ] リアルタイム監視
- [ ] Selenium系

---

## Phase 1: 安定運用（最優先）

### 1-1. Scheduler安定化

#### Task

* タスクスケジューラ設定の再整理
* WakeToRun 等の設定固定化
* XML exportバックアップ運用
* PowerShell再登録時の設定初期化対策

#### 成果物

* `scheduler-job-catalog.md` 更新
* scheduler export XML
* scheduler restore script

---

### 1-2. Scheduler Health監視

#### 新規Script

```text
scripts/check_scheduler_health.py
```

#### 役割

* 最終実行時刻監視
* 実行失敗検知
* 出力欠損検知
* Discord投稿確認

#### 出力

* `scheduler_health`
* Discord ALERTBOT通知

---

### 1-3. Missing補完強化

#### Task

* `check_daily_missing.py` 強化
* DB欠損検知追加
* prompt未生成検知
* Discord未投稿検知

---

# Phase 2: DB/データ基盤整理

### 2-1. SQLiteメンテナンス

#### 新規Script

```text
scripts/maintenance_sqlite.py
```

#### 役割

* VACUUM
* index整理
* orphan cleanup
* old log cleanup

---

### 2-2. DBスキーマ固定化

#### Task

* `entry_candidates`
* `signals`
* `backtest_outcomes`
* `execution_plan`
* `scheduler_health`

の定義整理

---

### 2-3. execution_plan table追加

#### 必須項目

| field     | 内容                    |
| --------- | --------------------- |
| symbol    | 銘柄                    |
| direction | LONG/SHORT            |
| entry     | Entry                 |
| tp        | TakeProfit            |
| sl        | StopLoss              |
| rr        | RR                    |
| ev        | ExpectedValue         |
| rank      | A/B/C                 |
| reasons   | 根拠                    |
| status    | pending/executed/skip |

---

# Phase 3: バックテスト強化

### 3-1. Outcome補完Script

#### 新規Script

```text
scripts/fill_backtest_outcomes.py
```

#### 役割

* paper trade取得
* OHLC参照
* T+1/T+5/T+20 outcome補完
* backtest_outcomes更新

---

### 3-2. Backtest Batch Runner

#### 新規Script

```text
scripts/run_backtest_range.py
```

#### 引数

```text
--from
--to
--max-trades
```

#### 処理

* scheduler実行
* paper trade登録
* outcome補完
* 統計更新

---

### 3-3. Rank性能分析

#### 新規Script

```text
scripts/evaluate_rank_performance.py
```

#### 出力

| rank | wr | ev | dd |
| ---- | -- | -- | -- |

---

### 3-4. Rule Calibration

#### Task

* A/B/C閾値調整
* Bランク検証
* EV補正調整
* RR閾値調整

---

# Phase 4: Execution運用

### 4-1. execution_plan生成

#### 新規Script

```text
scripts/build_execution_plan.py
```

#### 入力

* entry_candidates
* signal_rules
* rule_history

#### 出力

* entry
* tp
* sl
* rr
* ev
* reasons

---

### 4-2. Discordシナリオ改善

#### Task

朝通知内容整理：

| 項目    | 内容         |
| ----- | ---------- |
| 銘柄    | symbol     |
| 方向    | LONG/SHORT |
| Entry | 指値         |
| TP    | 利確         |
| SL    | 損切り        |
| RR    | リスクリワード    |
| EV    | 期待値        |
| 理由    | signals    |

---

### 4-3. Skip条件設計

#### Task

以下条件の除外ルール化：

* GU過熱
* 出来高不足
* 板薄
* 地合い悪化
* 特買長時間

---

# Phase 5: Market Context

### 5-1. Market Regime判定

#### 新規Script

```text
scripts/build_market_regime.py
```

#### 判定

* bullish
* bearish
* neutral

---

### 5-2. Gap戦略検証

#### Task

* GU/GD統計
* 宵越し検証

---

# Phase 6: 現行運用改善バックログ（2026-05-17）

## 6-1. 実装済み（確認済み）

* `mode=all` の成績比較（`backtest/watch/live`）を追加
  * script: `scripts/investment/backtest/analyze_paper_trade_stats.py`
* `paper-stats` の日次/週次自動更新を導入
  * `inv-evening` / `AIOS-Backtest-Weekly`
* `paper-stats` Discord通知を追加
  * render: `scripts/notify/render_paper_stats_discord_message.py`
  * post: `scripts/notify/post_paper_stats_discord.ps1`
* Scenario/Alert のタスク失敗を安定化
  * `AIOS-Inv-Scenario-0810` 正常化
  * `AIOS-Alert-Healthcheck` 正常化

## 6-2. 未完了タスク（優先順）

### P1. Ladder 実績検証（最優先）

* `strict / balanced / early / none` ごとの
  * 投稿数
  * entry率
  * T+5勝率 / 平均リターン
  を週次で集計する
* ラベル運用が有効かを数値で検証し、閾値調整の根拠にする
* 対象: `scripts/investment/backtest/generate_trade_watch_weekly_review.py`（拡張） or 専用script

### P1. `none` の2分割（監視優先度の明確化）

* `none-high-score` / `none-low-score` に分割し、WATCH監視の優先度を改善
* 投稿順と表示ラベルに反映
* 対象: `scripts/notify/post_scenarios_bot.py`

### P1. 返信系ガード強化（重複 entry 抑止）

* 同一銘柄の重複 `entry` に対して明示ACKを返し、誤操作を防止
* 既存 open trade との整合チェックを導入
* 対象: `scripts/notify/sync_scenario_replies_bot.py`

### P1. シナリオ投稿の重複抑止

* 同一銘柄・同一方向の短時間再投稿を抑止する
* 例: 直近N時間以内に同一キー投稿済みなら skip
* 対象: `scripts/notify/post_scenarios_bot.py`

### P1. 返信コマンドの堅牢化

* `entry lots=1 price=4022` 形式を受理
* 不正フォーマット時にエラーACK返信
* 対象: `scripts/notify/sync_scenario_replies_bot.py`

### P1. exit の根拠タグ追加

* `exit tp` / `exit sl` / `exit time` / `exit manual` を受理
* `paper_trades` または reply event に理由タグを保存
* 後続分析（勝率差・保有期間）で利用可能にする

### P2. paper-exit-timing の mode別比較化

* 1レポート内で `backtest/watch/live` を並列表記
* 対象: `scripts/investment/backtest/analyze_exit_timing.py`

### P2. 週次レビュー自動出力（1枚）

* trade/watch 勝率差、誤差、昇格候補を統合
* 出力例: `YYYY-MM-DD-weekly-trade-watch-review.md`

### P2. watch→trade 昇格ルールの明文化強化

* 既存の `min_samples / min_winrate / min_avg_ret` を docs に明記
* ルール変更履歴を `decisions.md` へ記録運用

### P3. 品質ゲートの自動調整案

* サンプル不足時に `min-winrate` を段階緩和
* 例: `n<20` の期間のみ暫定緩和、十分サンプル後に復帰
* 対象: `scripts/investment/signals/build_opening_scenarios.py`

### P3. 運用ドキュメント最終整備

* `doc/OPERATION_CURRENT.md`
* `doc/scheduler-job-catalog.md`
* `doc/script-role-catalog.md`
* 追加仕様（重複抑止/返信拡張/exitタグ/週次レビュー）を反映
* gap_up継続率
* gap fade分析

---

# Phase 6: 運用整理

### 6-1. Scriptカテゴリ整理

#### カテゴリ

| category | role      |
| -------- | --------- |
| collect  | 収集        |
| ingest   | DB投入      |
| evaluate | 分析        |
| execute  | execution |
| monitor  | 健全性       |
| repair   | 補完        |

---

### 6-2. script-role-catalog.md 更新

#### Task

* 新規script追加
* role分類追加
* 実行順整理

---

### 6-3. OPERATION_CURRENT.md 更新

#### Task

* 実運用フロー更新
* Discord運用反映
* backtestフロー反映

---

# Phase 7: 将来検討（後回し）

## 保留

* 板API
* リアルタイム監視
* Selenium系

---

# 現フェーズの最重要

優先順位：

```text
1. 毎日安定稼働
2. 欠損しない
3. outcome蓄積
4. execution_plan
5. ランク検証
```

リアルタイム性より：

「継続可能性」を優先。
