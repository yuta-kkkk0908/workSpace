# 課題メモ（2026-05-18）

## 今日の判断（重要）
- `scenario_messages` / `scenario_reply_events` 向けの実装は **見送り**。
- 理由: Discord 依存が強く、現運用では非構造化ログ（`discord-*.log`）で十分に回っているため。
- 方針: ingest 側だけで無理に復元する設計は採らない。

## 直近の残課題の扱い
元の「最終整備3点」は次のように更新する。

1. `post_scenarios_bot.py` / `sync_scenario_replies_bot.py` の JSONL 監査ログ出力追加
- 状態: **実施しない（保留）**
- メモ: 再開時に必要なら、Bot 側で `message_id/channel_id/reply_message_id/author_id` を JSONL 永続化する。

2. `ingest_investment_db.py` で `scenario_messages` / `scenario_reply_events` に upsert
- 状態: **実施しない（保留）**
- メモ: 1 が無い限り正確性が担保できないため進めない。

3. `check_daily_missing.py` に `topics.db` / `needs.db` を含む日次DB更新監視を統合
- 状態: **継続候補（必要になった時に着手）**
- メモ: 本件は Discord 依存が低く独立実装可能。

## 再開ポイント（次回ここから）
- `scenario_*` 系の実装は触らない前提で進行。
- 必要が出たら、まず「JSONL監査ログを本当に導入するか」を運用要件から再判断する。
- ドキュメント更新を先行し、実装開始前にこのファイルへ再開宣言を追記する。
